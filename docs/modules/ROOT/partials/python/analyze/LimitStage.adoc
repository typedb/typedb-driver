[#_LimitStage]
=== LimitStage

`Class(name=LimitStage, anchor=LimitStage, enumConstants=[], description=[Represents a ‘limit’ stage: limit &lt;limit&gt;], examples=[], fields=[], methods=[Method(name=limit, signature=limit() -> int, anchor=LimitStage_limit_, args=[], description=[], examples=[], mode=null, returnDescription=, returnType=int)], packagePath=typedb.api.analyze.pipeline_stage, superClasses=[PipelineStage], traitImplementors=[], mode=null).nameDescriptor(language)`

*Package*: `typedb.api.analyze.pipeline_stage`

*Supertypes:*

* `PipelineStage`

Represents a ‘limit’ stage: limit &lt;limit&gt;

// tag::methods[]
[#_LimitStage_limit_]
==== limit

[source,python]
----
limit() -> int
----



[caption=""]
.Returns
`int`

// end::methods[]

