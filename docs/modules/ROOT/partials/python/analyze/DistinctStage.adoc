[#_DistinctStage]
=== DistinctStage

`Class(name=DistinctStage, anchor=DistinctStage, enumConstants=[], description=[Represents a ‘distinct’ stage.], examples=[], fields=[], methods=[], packagePath=typedb.api.analyze.pipeline_stage, superClasses=[PipelineStage], traitImplementors=[], mode=null).nameDescriptor(language)`

*Package*: `typedb.api.analyze.pipeline_stage`

*Supertypes:*

* `PipelineStage`

Represents a ‘distinct’ stage.

