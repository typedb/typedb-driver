[#_Or]
=== Or

`Class(name=Or, anchor=Or, enumConstants=[], description=[Represents an ‘or’ constraint: { &lt;branches[0]&gt; } or { &lt;branches[1]&gt; } [or …]], examples=[], fields=[], methods=[Method(name=branches, signature=branches() -> Iterator['ConjunctionID'], anchor=Or_branches_, args=[], description=[Index into Pipeline.conjunctions], examples=[], mode=null, returnDescription=, returnType=Iterator['ConjunctionID'])], packagePath=typedb.api.analyze.constraint, superClasses=[Constraint], traitImplementors=[], mode=null).nameDescriptor(language)`

*Package*: `typedb.api.analyze.constraint`

*Supertypes:*

* `Constraint`

Represents an ‘or’ constraint: { &lt;branches[0]&gt; } or { &lt;branches[1]&gt; } [or …]

// tag::methods[]
[#_Or_branches_]
==== branches

[source,python]
----
branches() -> Iterator['ConjunctionID']
----

Index into Pipeline.conjunctions

[caption=""]
.Returns
`Iterator['ConjunctionID']`

// end::methods[]

