[#_Credentials]
=== Credentials

`Class(name=Credentials, anchor=Credentials, enumConstants=[], description=[User credentials and TLS encryption settings for connecting to TypeDB Server.], examples=[credentials = Credentials(username, password)], fields=[], methods=[], packagePath=typedb.api.connection.credentials, superClasses=[], traitImplementors=[], mode=null).nameDescriptor(language)`

*Package*: `typedb.api.connection.credentials`

User credentials and TLS encryption settings for connecting to TypeDB Server.

[caption=""]
.Examples
[source,python]
----
credentials = Credentials(username, password)
----

