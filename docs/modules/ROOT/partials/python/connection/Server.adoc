[#_Server]
[.doc-api-reference-driver]
=== Server

`class`

*Package*: `typedb.api.server.server`

The metadata and state of an individual raft server of a driver connection.

[caption=""]
.Properties
// tag::properties[]
[cols=",,"]
[options="header"]
|===
|Name |Type |Description
a| `address` a| `str` a| Returns the address this server is hosted at.
a| `id` a| `int` a| Returns the id of this server.
a| `role` a| `ReplicationRole \| None` a| Returns whether this is the primary server of the raft cluster or any of the supporting types.
a| `term` a| `int` a| Returns the raft protocol 'term' of this server.
|===
// end::properties[]

// tag::methods[]
[#_Server_is_primary_]
==== method ``is_primary``

[source,python]
----
is_primary() -> bool
----

Checks whether this is the primary server of the raft cluster.

[caption=""]
.Returns
`bool`

[caption=""]
.Code examples
[source,python]
----
server.is_primary()
----

// end::methods[]

